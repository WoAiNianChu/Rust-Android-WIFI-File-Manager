plugins {
    id 'com.android.application'
}
android {
    namespace 'psycho.euphoria.killer'
    compileSdk 32

    defaultConfig {
        applicationId "psycho.euphoria.killer"
        minSdk 26
        targetSdk 32
        versionCode 1
        versionName "1.0"
        ndk.abiFilters 'arm64-v8a'
        
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
}

dependencies {
    // https://github.com/arthenica/ffmpeg-kit
    // Upstream ffmpeg-kit artifacts were retired and are no longer resolvable from Maven Central.
    // Pin to the upstream ffmpeg-kit repository via JitPack and keep the existing full-gpl variant.
    implementation 'com.github.arthenica:ffmpeg-kit-full-gpl:v6.0'
}
